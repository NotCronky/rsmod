package gg.rsmod.plugins.content.areas.misthalin.barbarianvillage.strongholdofsecurity.objs

on_obj_option(obj = 20786, option = "use") {
    player.queue {
        wait(2)
        player.moveTo(x = 1903, z = 5222)
    }
}

on_obj_option(obj = 19005, option = "use") {
    player.queue {
        wait(2)
        player.moveTo(x = 2026, z = 5217)
    }
}

on_obj_option(obj = 23707, option = "use") {
    player.queue {
        wait(2)
        player.moveTo(x = 2148, z = 5283)
    }
}

on_obj_option(obj = 23922, option = "use") {
    player.queue {
        wait(2)
        player.moveTo(x = 2349, z = 5215)
    }
}